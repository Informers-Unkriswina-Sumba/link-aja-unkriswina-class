import React, { useState, Fragment } from "react";

interface Props {

}

const Button: React.FC<Props> = (props) => {

  return (
    <div>
      Button Component
    </div>
  );
};

export default Button;